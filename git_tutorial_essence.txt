- with git diff = diffs between working dir and repo ((and staging) aka things that are unique to the wdir)
- with git diff --staged = diffs between working dir and staging
- how to deal with the rename